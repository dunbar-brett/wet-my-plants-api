version: "3"
services:
  database:
    image: postgres
    env_file:
      - database.env
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - database-data:/var/lib/postgresql/data/